/**
 * config - should contain db configuration
 * controller - should contain logic/actions performed on the backend
 * routes - specifies routes that interact with frontend
 * model - implentation for your database tables or schemas
 * view - this is the react application/ frontend of your app (it resides in the backend as the view mVc architecture)
 */

// server.js - this is the entry or configuration point for your server

// kindly transfer your html implementation to App.js file in the [view]